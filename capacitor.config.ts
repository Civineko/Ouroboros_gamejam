import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.civineko.quanyiquan",
  appName: "圈一圈",
  webDir: "dist",
  backgroundColor: "#48678f",
  android: {
    backgroundColor: "#48678f",
  },
};

export default config;
